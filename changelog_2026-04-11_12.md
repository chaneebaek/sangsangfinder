# Changelog: 2026-04-11 ~ 2026-04-12

## 개요

QA 데이터셋 생성 파이프라인 구축 및 전체 공지(469건)에 대한 데이터셋 생성을 완료했습니다.
총 **2,345개 QA 쌍**이 생성됐으며 (`qa_dataset_all.jsonl`), 로컬에만 보관됩니다 (gitignore).

---

## 커밋 내역

### 2026-04-11

#### `chore`: teacher-student QA 생성 관련 코드 삭제 (`fab087b`)
기존 Teacher-Student 구조에서 Student 관련 코드를 제거했습니다.

#### `chore`: Qwen2.5-7B QA 생성 파이프라인 삭제 (`a3492db`)
Qwen2.5-7B 기반 QA 생성 파이프라인을 제거했습니다.

#### `feat`: Teacher-Judge QA 파이프라인 추가 (`86c565c`)
Claude API 기반의 Teacher-Judge QA 생성 파이프라인(`run_qa_pipeline.py`)을 새로 구축했습니다.

- **Teacher** (`claude-sonnet-4-6`): 공지당 QA 5개 생성 (well-formed 3개 + 유저형 2개)
- **Judge** (`claude-haiku-4-5`): hallucination 탐지 + 품질 점수 2단계 검증
- QA 통과 즉시 파일에 flush하여 중단 시 데이터 보존

---

### 2026-04-12

#### `refactor`: Teacher-only 파이프라인으로 전환 (`2f12993`)
운영 과정에서 발견된 문제를 수정하고 파이프라인을 개선했습니다.

- `thinking={"type": "adaptive"}` 파라미터 제거 (Sonnet 4.6 무한 대기 버그 원인)
- `run_pipeline`에 `append` 모드 추가 → 크레딧 소진 등 중단 후 이어서 실행 가능
- Judge 2단계 검증 코드 주석처리 (코드 보존, 재활성화 가능)
- 모든 `print`에 `flush=True` 적용 → 실시간 진행 상황 출력

#### `chore`: 미반영 변경사항 일괄 커밋 (`f66d51c`)
- `app.py`, `colab_crawl.py`, `train.py`, `CLAUDE.md` 업데이트
- `embedding_search_v1.py`, `test.py` 삭제
- `crawl_2025_titles.json` 추가
- `notices_cache.json` 삭제 (`qa_dataset_generation/data/` gitignore 적용)

#### `chore`: `.DS_Store` gitignore 추가 (`55fd175`)

---

## QA 데이터셋 생성 결과

| 파일 | 공지 범위 | QA 수 |
|------|-----------|-------|
| `qa_dataset_full.jsonl` | 1 ~ 274번 | 1,370개 |
| `qa_dataset_275_469.jsonl` | 275 ~ 469번 | 975개 |
| `qa_dataset_all.jsonl` | 전체 (병합) | **2,345개** |

> 데이터 파일은 `qa_dataset_generation/data/` 경로에 로컬 보관 (gitignore)
